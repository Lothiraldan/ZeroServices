============
Installation
============

At the command line::

    $ easy_install zeroservices

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv zeroservices
    $ pip install zeroservices
